# Node.js
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript
*.tsbuildinfo

# Logs and local data
logs/
*.log
**/reports
midnight-level-db
coverage

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of `npm pack`
*.tgz

# Yarn Integrity file
.yarn-integrity

# Environment — do not commit secrets (templates use `.env_template`)
.env
.env.*
!.env_template

# MacOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Cursor (local workspace / rules; not part of the template repo)
.cursor/

# VSCode — allow shared editor config only if present
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# IDEs and editors
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Turbo
.turbo

# Vercel CLI / preview deployments
.vercel

# Vite (occasional transient config caches)
*.timestamp-*

# Frontend build artifacts (copied from contract packages during `copy-contract-keys`)
frontend-vite-react/public/midnight/
